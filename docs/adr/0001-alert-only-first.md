# ADR 0001: Alert-only first release

Status: accepted. The scanner consumes public market data and emits persisted recommendations. It holds no exchange credentials and places no orders, isolating signal validation from execution risk.
